apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  namespace: default
  name: developer
rules:
  - apiGroups: [ "" ]
    verbs: [ "create", "get", "list", "update", "delete" ]
    resources: [ "pods" ]
